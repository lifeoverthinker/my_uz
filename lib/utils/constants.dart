// Plik: lib/utils/constants.dart
// import 'package:flutter/material.dart'; // <-- USUNIĘTO NIEUŻYWANY IMPORT

// Odstęp używany w detalach (Class, Task) między ikoną a tekstem
const double kIconToTextGap = 12.0;

// Szerokość "slotu" na ikonę w widokach detali
const double kIconSlotWidth = 40.0;

// Obliczony padding dla formularzy, aby zrównać się z tekstem w detalach
const double kFormContentPadding = kIconSlotWidth + kIconToTextGap; // 52.0